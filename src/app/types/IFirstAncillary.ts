export interface IFirstAncillary {
  name: string;
  apiVersion: string;
  status: string;
  serviceManifestName: string;
  serviceManifestVersion: string;
  codePackageName: string;
  codePackageVersion: string;
  currentSeason: number;
  maxSeason: number;
}
